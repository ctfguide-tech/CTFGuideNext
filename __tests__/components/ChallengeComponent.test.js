import { render, screen } from '@testing-library/react';
import ChallengeComponent from '../../components/challenge/ChallengeComponent';

describe('ChallengeComponent component', () => {
  test('renders without crashing', () => {
    render(<ChallengeComponent />);
    // Add more assertions here to test the functionality of the ChallengeComponent component
  });

  // Add more test cases as needed
});